{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Spotlight for Windows",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "global-shortcut:default",
    "shell:default"
  ]
}
